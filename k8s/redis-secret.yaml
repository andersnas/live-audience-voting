# Apply with real password — never commit with actual values:
#   kubectl create secret generic redis-secret \
#     --from-literal=password=YOUR_REDIS_PASSWORD
#
# Template only — do not apply this file directly:
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
type: Opaque
stringData:
  password: G7HBnj9BRP3Yt
